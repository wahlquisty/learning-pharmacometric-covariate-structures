\pgfplotsset{
    ScatterStyle/.style={
        scale only axis,
        height = 60mm,
        width = 60mm,
        xlabel = {Predicted concentration [$\mu$g/ml]},
        ylabel = {Observed concentration [$\mu$g/ml]},
        xmin = 0,
        ymin= 0,
        xmax = 42,
        ymax= 42,
        %legend style={font=\small, at={(0.98,0.22)},anchor=east},
        line width = 0.5pt,
        %axis line style={draw=none}
        axis on top,
        clip mode=individual,
    },
    HistogramStyle/.style={
    bar width=5mm,
    ymin=0,
    xmin = 0,
    xmax = 110,
    scale only axis,
    width = 60mm,
    height = 50mm,
    ylabel = {$\#$ of patients},
    },
    BoxplotStyle/.style ={
    scale only axis,
    width = 60mm,
    height = 40mm,
    boxplot/draw direction = y,
    % xtick = {1,1.3,1.6,1.9},
    % xticklabels = {Symb. regr.,Eleveld, Individual, Population},
    % xmin = 0.5,
    % xmax = 2.5,
    every y tick label/.append style={font=\footnotesize},
    every x tick label/.append style={font=\footnotesize},
    boxplot/box extend=0.5 % <-- added
    % /pgfplots/boxplot/box extend = 0.2,
    % ylabel = {\vspace{0mm} MSE},
    % xlabel = {}
    },
}